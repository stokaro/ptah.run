{
  "name": "ptah-landing-checks",
  "private": true,
  "scripts": {
    "check": "node scripts/build-runs.mjs --selftest && node scripts/build-runs.mjs --check && node scripts/check-locales.mjs --selftest && node scripts/check-locales.mjs",
    "check:browser": "node scripts/check-browser.mjs"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
