---
import FragmentPage from "../../../components/pages/FragmentPage.astro";
import { PREFIXED } from "../../../lib/site.mjs";

export function getStaticPaths() {
  return PREFIXED.map((lang) => ({ params: { lang } }));
}
const { lang } = Astro.params;
---
<FragmentPage lang={lang} page="install" />
